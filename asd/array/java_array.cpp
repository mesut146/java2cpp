#include "java_array.h"
#include <iostream>

/*template <typename T> java_array<T>::java_array(){
    this->length=0;
    this->elems=nullptr;
}*/

/*template <class T> java_array<T>::java_array(T *arr, int s) {
    elems = new T[s];
    length = s;
    for(int i = 0; i < length; i++){
        elems[i] = arr[i];
    }
}*/

/*template <class T> void java_array<T>::init(){
    std::cout<<"init"<<std::endl;
}*/

/*template <typename T> T& java_array<T>::operator[] (const int& index) const{
    if(index>=0&&index<length){
        return elems[index];
    }else{
        throw 0;
    }
}*/
